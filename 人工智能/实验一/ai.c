/**
	 * Author 刘老师
	 * Function 根据用户输入特征数判断动物品种
	 * Version 1.0
	 * Date 2021.9.28
	 */ 

#include<stdio.h>
#define N 23    //定义一个常量来判断是否退出while循环

int main()
{
	// 定义后面循环要用到的变量
	int i,j,k,a,b,c;
	// 定义一个int变量存储特征数
	int num;
	// 定义两个数组存储用户输入的特征数和判断动物的特征数
	int fact[N],temp[N];
	// 定义一个int变量判断用户是否退出程序
	int flag=1;
	
	while(flag==1)
	{
	//提示用户输入
	printf("动物的特征如下:\n");
	printf("1有奶  2有毛发  3有羽毛  4会飞  5生蛋\n6有爪  7有犬齿  8目盯前方  9吃肉  10有蹄\n11反刍食物  12黄褐色  13黑色条纹  14黑色斑点  15长腿\n16长脖子  17暗斑点  18白色   19不会飞  20黑白色\n21会游泳  22善飞  23不怕风浪\n");
	printf("请输入描述该动物特征的个数:");
	//读取用户输入
	scanf("%d",&num);
	printf("请输入对这只动物的特征描述的序号(按序号由小到大):\n");
	//把特征数存到fact数组里面
	for(i=0;i<num;i++)
	{
		scanf("%d",&a);
		fact[i]=a;
	}
	//根据输入条件该动物判断该动物是否为哺乳动物	
	for(i=0;i<num;i++)
	{
		if(fact[i]==1)
{
			fact[num]=24;
			num++;
			printf("使用规则1,新增加的事实为: 哺乳动物\n");
			break;
		}
	}
	//根据输入条件该动物判断该动物是否为哺乳动物	
	for(i=0;i<num;i++)
	{
		if(fact[i]==2)
		{
			fact[num]=24;
			num++;
			printf("使用规则2,新增加的事实为: 哺乳动物\n");
			break;
		}
	}
	//根据输入条件该动物判断该动物是否为鸟	
	for(i=0;i<num;i++)
	{
		if(fact[i]==3)
		{
			fact[num]=25;
			num++;
			printf("使用规则3,新增加的事实为:鸟\n");
			break;
		}
	}
	//根据输入条件该动物判断该动物是否为鸟
	k=0;
	for(i=0;i<num;i++)
	{
		if(fact[i]==4)
		{
			temp[k]=fact[i];
			k++;
			continue;
		}
		if(fact[i]==5)
		{
			temp[k]=fact[i];
			break;
		}
	}
	if(temp[0]==4&&temp[1]==5)
	{
		fact[num]=25;
		num++;
		printf("使用规则4,新增加的事实为:鸟\n");
	}
	//根据输入条件该动物判断该动物是否为食肉动物
	k=0;
	for(i=0;i<num;i++)
	{
		// 把输入特征数存到temp数组，以便后面的if判断
		if(fact[i]==6)
		{
			temp[k]=fact[i];
			k++;
			continue;
		}
		if(fact[i]==7)
		{
			temp[k]=fact[i];
			k++;
			continue;
		}
		if(fact[i]==8)
		{
			temp[k]=fact[i];
			k++;
			continue;
		}
		if(fact[i]==24)
		{
			temp[k]=fact[i];
			break;
		}
	}
	// 根据temp数组条件判断是否为食肉动物
	if(temp[0]==6&&temp[1]==7&&temp[2]==8&&temp[3]==24)
	{
		fact[num]=26;
		num++;
		printf("使用规则5,新增加的事实为:食肉动物\n");
	}	
	k=0;
	for(i=0;i<num;i++)
	{
		if(fact[i]==9)
		{
			temp[k]=fact[i];
			k++;
			continue;
		}
		if(fact[i]==24)
		{
			temp[k]=fact[i];
			break;
		}
	}
	if(temp[0]==9&&temp[1]==24)
	{
		fact[num]=26;
		num++;
		printf("使用规则6,新增加的事实为:食肉动物\n");
	}
	//根据输入条件该动物判断该动物是否为有蹄动物	
	k=0;
	for(i=0;i<num;i++)
	{
		if(fact[i]==10)
		{
			temp[k]=fact[i];
			k++;
			continue;
		}
		if(fact[i]==24)
		{
			temp[k]=fact[i];
			break;
		}
	}
	if(temp[0]==10&&temp[1]==24)
	{
		fact[num]=27;
		num++;
		printf("使用规则7,新增加的事实为:有蹄动物\n");
	}
	//根据输入条件该动物判断该动物是否为偶蹄动物	
	k=0;
	for(i=0;i<num;i++)
	{
		if(fact[i]==11)
		{
			temp[k]=fact[i];
			k++;
			continue;
		}
		if(fact[i]==27)
		{
			temp[k]=fact[i];
			break;
		}
	}
	if(temp[0]==11&&temp[1]==27)
	{
		fact[num]=28;
		num++;
		printf("使用规则8,新增加的事实为:偶蹄动物\n");
	}
	//根据输入条件该动物判断该动物是否为老虎
	k=0;
	for(i=0;i<num;i++)
	{
		if(fact[i]==12)
		{
			temp[k]=fact[i];
			k++;
			continue;
		}
		if(fact[i]==13)
		{
			temp[k]=fact[i];
			k++;
			continue;
		}
		if(fact[i]==24)
		{
			temp[k]=fact[i];
			break;
		}
	}
	if(temp[0]==12&&temp[1]==13&&temp[2]==24)
	{
		fact[num]=30;
		//num++;
		printf("使用规则9,新增加的事实为:老虎\n该动物为老虎\n");
	}
	//根据输入条件该动物判断该动物是否为金钱豹
	k=0;
	for(i=0;i<num;i++)
	{
		if(fact[i]==12)
		{
			temp[k]=fact[i];
			k++;
			continue;
		}
		if(fact[i]==14)
		{
			temp[k]=fact[i];
			k++;
			continue;
		}
		if(fact[i]==24)
		{
			temp[k]=fact[i];
			break;
		}
	}
	if(temp[0]==12&&temp[1]==14&&temp[2]==24)
	{
		fact[num]=31;
		//num++;
		printf("使用规则10,新增加的事实为:金钱豹\n该动物为金钱豹\n");
	}
	//根据输入条件该动物判断该动物是否为长颈鹿
	k=0;
	for(i=0;i<num;i++)
	{
		if(fact[i]==12)
		{
			temp[k]=fact[i];
			k++;
			continue;
		}
		if(fact[i]==15)
		{
			temp[k]=fact[i];
			k++;
			continue;
		}
		if(fact[i]==16)
		{
			temp[k]=fact[i];
			k++;
			continue;
		}
		if(fact[i]==17)
		{
			temp[k]=fact[i];
			k++;
			continue;
		}
		if(fact[i]==27)
		{
			temp[k]=fact[i];
			break;
		}
	}
	if(temp[0]==12&&temp[1]==15&&temp[2]==16&&temp[3]==17&&temp[4]==27)
	{
		fact[num]=32;
		//num++;
		printf("使用规则11,新增加的事实为:长颈鹿\n该动物为长颈鹿\n");
	}
	//根据输入条件该动物判断该动物是否为斑马 
	k=0;
	for(i=0;i<num;i++)
	{
		if(fact[i]==13)
		{
			temp[k]=fact[i];
			k++;
			continue;
		}
		if(fact[i]==18)
		{
			temp[k]=fact[i];
			k++;
			continue;
		}
		if(fact[i]==27)
		{
			temp[k]=fact[i];
			break;
		}
	}
	if(temp[0]==13&&temp[1]==18&&temp[2]==27)
	{
		fact[num]=33;
		//num++;
		printf("使用规则12,新增加的事实为:斑马\n该动物为斑马\n");
	}
	//根据输入条件该动物判断该动物是否为鸵鸟	
	k=0;
	for(i=0;i<num;i++)
	{
		if(fact[i]==15)
		{
			temp[k]=fact[i];
			k++;
			continue;
		}
		if(fact[i]==16)
		{
			temp[k]=fact[i];
			k++;
			continue;
		}
		if(fact[i]==19)
		{
			temp[k]=fact[i];
			k++;
			continue;
		}
		if(fact[i]==25)
		{
			temp[k]=fact[i];
			break;
		}
	}
	if(temp[0]==15&&temp[1]==16&&temp[2]==19&&temp[3]==25)
	{
		fact[num]=34;
		//num++;
		printf("使用规则13,新增加的事实为:鸵鸟\n该动物为鸵鸟\n");
	}
	//根据输入条件该动物判断该动物是否为企鹅	
	k=0;
	for(i=0;i<num;i++)
	{
		if(fact[i]==19)
		{
			temp[k]=fact[i];
			k++;
			continue;
		}
		if(fact[i]==20)
		{
			temp[k]=fact[i];
			k++;
			continue;
		}
		if(fact[i]==21)
		{
			temp[k]=fact[i];
			k++;
			continue;
		}
		if(fact[i]==25)
		{
			temp[k]=fact[i];
			break;
		}
	}
	if(temp[0]==19&&temp[1]==20&&temp[2]==21&&temp[3]==25)
	{
		fact[num]=35;
		//num++;
		printf("使用规则14,新增加的事实为:企鹅\n该动物为企鹅\n");
	}
	//根据输入条件该动物判断该动物是否为海燕	
	k=0;
	for(i=0;i<num;i++)
	{
		if(fact[i]==22)
		{
			temp[k]=fact[i];
			k++;
			continue;
		}
		if(fact[i]==23)
		{
			temp[k]=fact[i];
			k++;
			continue;
		}
		if(fact[i]==25)
		{
			temp[k]=fact[i];
			break;
		}
	}
	if(temp[0]==22&&temp[1]==23&&temp[2]==25)
	{
		fact[num]=29;
		//num++;
		printf("使用规则15,新增加的事实为:海燕\n该动物为海燕\n");
	}
	//根据之前的if条件，如果都不满足那些条件询问用户是否退出while 
	if(fact[num]<29)
	printf("现有事实无法推断出结果!\n");
	printf("\n");
	printf("继续请按1,退出按其它数字键:");
	scanf("%d",&c);
	if(c==1) 
		flag=c;
	else break;
	}
	return 0;
}
